# Deployed at /opt/kafka-connect/config/connect-distributed.properties on the
# Kafka Connect EC2 instance (i-08cc1464acf2828a2). Kept here as the
# source-of-truth reference copy - see TASK_KAFKA_CONNECT_SETUP.md.
bootstrap.servers=10.42.2.152:9092
group.id=iot-hackathon-connect-cluster

config.storage.topic=connect-configs
config.storage.replication.factor=1

offset.storage.topic=connect-offsets
offset.storage.replication.factor=1

status.storage.topic=connect-status
status.storage.replication.factor=1

key.converter=org.apache.kafka.connect.json.JsonConverter
value.converter=org.apache.kafka.connect.json.JsonConverter
key.converter.schemas.enable=false
value.converter.schemas.enable=false

offset.flush.interval.ms=10000

listeners=HTTP://0.0.0.0:8083
rest.advertised.host.name=127.0.0.1
rest.advertised.port=8083

plugin.path=/opt/kafka-connect/plugins
