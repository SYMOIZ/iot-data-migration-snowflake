[Unit]
Description=IoT Analytics Streamlit Dashboard
After=network.target

[Service]
Type=simple
WorkingDirectory=/opt/streamlit/app
ExecStart=/opt/streamlit-venv/bin/streamlit run Home.py --server.headless=true
Restart=on-failure
RestartSec=5
User=root

[Install]
WantedBy=multi-user.target
